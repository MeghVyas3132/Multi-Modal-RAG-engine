# ================================================================
# Secret -- Sensitive configuration (base64-encoded in production)
# In real deployments, use External Secrets Operator or Sealed Secrets.
# ================================================================
apiVersion: v1
kind: Secret
metadata:
  name: rag-secrets
  namespace: rag-system
  labels:
    app.kubernetes.io/part-of: clip-image-search
type: Opaque
stringData:
  # API keys for authentication (comma-separated)
  API_KEYS: ""
  # OpenAI API key for LLM explanations
  OPENAI_API_KEY: ""
